# KB Implementation Root

This directory is the implementation root for consumers of the passive MKF contract in `skills/src/interface/knowledge-base/`.

Implementation behavior is intentionally deferred from the structural refactor. New KB code must depend on the skills-owned contract rather than redefining it.
