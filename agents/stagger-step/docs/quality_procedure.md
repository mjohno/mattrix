This is a quality procedure for the project.

Rules:
- Treat each step as its own task.
- Run from the root of this project (where `make.py` is located).

Instructions:
1. `python3 make.py quality`
2. `python3 make.py format`
3. `python3 make.py build-stagger-step`
4. `python3 make.py docker-build`
