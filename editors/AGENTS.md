# Editors

This domain owns editor configuration.

## Rules

- Keep editor configuration self-contained.
- Do not add editor-specific dependencies to `skills/`, `kb/`, or `agents/`.
- Keep operating-system-specific configuration isolated.
- Document required external tools in each editor README.
